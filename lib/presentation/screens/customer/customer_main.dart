import 'package:flutter/material.dart';

class CustomerMain extends StatelessWidget {
  static const String id = '/cutomer_main';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('customer main'),
    );
  }
}
